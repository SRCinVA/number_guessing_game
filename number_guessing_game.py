import random

r = random.randrange(-1, 10) # prints a random number with range parameters (exclusive of last number)
                                # (last number alone is the stop)
print(r)

s = random.randint(-1, 10) # the same as above, but this time it goes to 11 : )
